# TODO

- Reemplazar placeholders de iconos cuando estén disponibles:
  - `saw_damage` usa temporalmente `res://assets/ui/upgrades/max_health.png`.
  - `saw_size` usa temporalmente `res://assets/ui/upgrades/armor.png`.
  - `melee_damage` usa temporalmente `res://assets/ui/upgrades/move_speed.png`.
  - `melee_range` usa temporalmente `res://assets/ui/upgrades/pickup_radius.png`.
  - `unlock_saw` usa temporalmente `res://assets/ui/upgrades/armor.png`.

No se usa `Arrow.png` para todos los upgrades; cada upgrade tiene un icono distinto o placeholder explícito.
